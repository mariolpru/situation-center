# История и грабли

## ✅ Сделано (бывший TODO — закрыт)

Все три «прерванные» задачи доведены до конца и в main:

1. **Ошибки заполнения полей** (Figma 3285:12385) — `commit f62482b`, v16. `.field-wrap`/`.field-error`/`.is-invalid`, `setFieldError()`; применено к формам лекции, экскурсии, рассылки.
2. **Валидация email + попап подписки** (3273:5892) — `commit f62482b`, v16. `subscribeFlow()`: пусто → «Это поле нужно обязательно заполнить», невалидно → «Введите корректный email латиницей»; валидный сабмит → динамическая модалка «Подписка на новости оформлена» с подстановкой email (regex `^[A-Za-z0-9._%+-]+@[A-Za-z0-9-]+(\.[A-Za-z0-9-]+)+$`).
3. **5-шаговый флоу экскурсии** (3273:5890) — `commit f62482b`, v16. `excursionFlow()`: дата (календарь июль 2026, Сб/Вс красные, disabled прошедшие/занятые, «сегодня» обведён) → время (слоты 11:00–20:00, 5 колонок) → кол-во (степпер 1–20 + «Продолжить») → форма (ФИО/Email/Тел + сводка «дата · время · N чел.») → «Ваша запись принята». Занятость — детерминированный псевдорандом от дня/часа. Закрытие = сброс к шагу 1.

Плюс сверх плана: **интерактивные зоны 2-го этажа панорамы** — `commit 41cb246`, v18 (6 хотспотов → модалки «Зал»/«Игра»/«Панорама»).

## Известные грабли

- Классификатор Bash-команд иногда падает («temporarily unavailable») — переждать или использовать Write/Edit вместо больших bash-heredoc.
- Мусорный `assets/img/f1926de6…svg` — иконка крестика из последнего экспорта, можно удалить.
- TT Bluescreens — Trial-лицензия: для продакшена нужна коммерческая.
- SVG из Figma приходят с `width="100%"` — прописывать явные размеры из viewBox (иначе `<img>` едет).
- `max.ru/share?url=` — предположительный формат шаринга Макса (публичного эндпоинта нет).
